# AI绘画 Tag 工具箱模块划分与接口调用报告

版本：V1.4.191  
范围：`app/` 实际源码

## 一、分层结构

项目可以分为五层：

1. **桌面宿主层**：`main.js`、`preload.js`。负责 Electron 窗口、安全边界、数据迁移和模块组装。
2. **页面表现层**：`src/index.html`、`src/app.css`、`src/app.js`、`src/app-view.js`。负责路由、DOM 渲染、事件和用户操作。
3. **业务领域层**：标签、图片、画廊/会话引用、识图、翻译、提示词、ComfyUI。
4. **AI 编排层**：`assistant.js`、`ai-runner.js`。负责 AI 请求、会话、工具循环和助手/绘图模式。
5. **工具协议层**：`calls/index.js`、`call-table.js`、`call-protocol.js`、`calls/server.js`。负责工具注册、schema、权限、执行和外部 Agent HTTP 接入。

依赖方向总体为：

```text
页面 -> preload API -> 业务/编排模块 -> storage 或外部服务
                                      ├─ 本地文件/模型
                                      ├─ OpenAI 兼容 API
                                      └─ ComfyUI API
```

业务模块不依赖 DOM；页面只能通过 preload 暴露的受控接口调用业务能力。

## 二、模块职责与公开接口

| 模块 | 文件 | 主要职责 | 主要接口/输出 |
|---|---|---|---|
| Electron 主进程 | `main.js` | 创建窗口、旧版本迁移、外部链接安全处理 | `createWindow()`、`migrateLegacyUserData()` |
| Preload 组装层 | `preload.js` | 创建实例、读取素材/模型、暴露 `window.aiTag` | 标签、图片、AI、Vision、翻译、Comfy、存储 API |
| 存储 | `modules/storage.js` | JSON 键值持久化、导入导出 | `createStorage()`、`get/set/remove/clear/export/import` |
| 标签 | `modules/tags.js` | 标签目录、分类、搜索、选择、自定义标签 | `createTags()`、分类/搜索/选择/自定义标签方法 |
| 图片资产 | `modules/images.js` | 图片字节、data URL、缩略图、PNG 元数据、分析结果 | `createImages()`、`add/get/remove/list/analyze/getBytes` |
| 图片引用仓库 | `modules/image-repository.js` | 画廊、会话图片引用、pending、孤儿回收 | `attachToConversation()`、`addToGallery()`、`referenceCount()`、导入导出 |
| 本地 Vision | `modules/vision.js` | WD 模型加载和本地 Tag 推理 | `createVision()`、`analyze()` |
| 统一 Vision | `modules/vision-service.js` | 统一本地/远程/元数据识图入口 | `processOne({ imageId, mode })` |
| 翻译 | `modules/translation.js` | 中英方向判断、本地匹配、本地模型/AI 翻译 | `createTranslation()`、`translate()` |
| 提示词 | `modules/prompts.js` | 素材读取、自定义提示词、启用状态、导入导出 | `list/get/createCustom/update/remove/export/import` |
| ComfyUI | `modules/comfy.js` | 连接检测、工作流分析、参数覆盖、提交和下载图片 | `test/inspect/render/fetchImage` |
| AI 助手 | `modules/assistant.js` | AI 配置、会话、消息、AiService、模式上下文 | `createAssistant()`、`send/stop/session/config` |
| AI 编排器 | `modules/ai-runner.js` | 助手/绘图共用工具循环 | `createAiRunner()`、运行任务、流式事件 |
| 工具定义 | `modules/call-table.js` | 工具名称、参数 schema、权限元数据 | `getCallDefinition/listCallDefinitions` |
| 工具注册与执行 | `modules/calls/index.js` | 注册 handler、能力刷新、调用和 OpenAI tools | `createCalls()`、`listAvailable/refreshCapabilities/call/has` |
| 调用解析 | `modules/call-protocol.js` | 解析模型 JSON 调用、图片引用、执行摘要 | `extractAssistantCalls/execute/planImageContext` |
| 外部 Agent 服务 | `modules/calls/server.js` | 本地 HTTP 工具服务 | `createCallServer().start/stop` |
| 旧数据迁移 | `migrate.js`、`config-migration.js` | 旧键、旧会话、旧图片集合迁移 | `migrateLegacyData()`、迁移标记 |

## 三、启动时的接口组装顺序

`preload.js` 按以下顺序初始化：

1. 加载 `src/modules/index.js` 汇总导出。
2. 从 `assets/数据资产/标签` 加载标签文件，创建 `storage` 和 `tags`。
3. 搜索 `models/tags-canary.json`，创建 `vision`。
4. 创建 `images`，并注入 `vision.analyze` 作为图片分析器。
5. 创建 `prompts`，读取 `assets/提示词素材`。
6. 创建 `comfy`，默认地址为 `http://127.0.0.1:8188`。
7. 创建 `assistant`，注入 `tags/images/vision/comfy/storage/prompts`。
8. Assistant 内部创建 Calls 注册表和 `imageRepository`。
9. 创建外部 Agent `callServer`，默认监听 `32145`。
10. 创建 `translation`，注入 `tags`、本地翻译 runner 和 `assistant.ai`。
11. 执行剩余旧会话/图片迁移并完成引用修复。

因此，Assistant 是 AI 相关能力的组合中心；Images、Tags、Vision、Comfy 等是可被多个入口复用的领域服务。

## 四、核心接口调用关系

### 1. 页面到业务模块

```text
app-view.js
  -> window.aiTag.tags       标签搜索/选择/收藏
  -> window.aiTag.images     上传、预览、元数据
  -> window.aiTag.vision     本地识图和 Vision Service
  -> window.aiTag.translation 翻译和 Tag 匹配
  -> window.aiTag.assistant  会话、发送、停止、设置
  -> window.aiTag.comfy      工作流测试/导入/同步
  -> window.aiTag.storage    UI 偏好设置
```

`app.js` 负责初始化和路由，`app-view.js` 负责具体事件；页面不直接调用 Node 文件系统。

### 2. AI 助手调用链

```text
页面 send
  -> assistant.send/run
      -> ai-runner.run
          -> assistant.ai.complete/stream
          -> calls.refreshCapabilities
          -> calls.openAiTools
          -> AI 返回 tool call
          -> call-protocol.normalise/execute
          -> calls.call(executor, args)
          -> tool result 回填消息
          -> 继续 AI 请求直到完成
      -> imageRepository.attach/markSent
      -> 会话 storage 持久化
```

助手模式通常使用 Tag、Vision、提示词读取等 Runtime 工具；绘图模式额外允许 `comfy.render`，但只有 Comfy 已连接、工作流有效且用户启用实际渲染时才执行。

### 3. Vision 调用链

```text
页面/AI/Agent
  -> calls.call('vision.processOne', { imageId, mode })
      -> vision-service.processOne
          -> images.get/getBytes 或临时文件
          -> mode=metadata：读取图片元数据
          -> mode=local：vision.analyze
          -> mode=ai：assistant.ai Vision 请求
          -> 标准化标签、置信度、描述和错误
      -> 返回结果并可写回 images.analysis
```

统一入口要求明确 `imageId`，避免把任意路径或外部 URL 当作本地图片访问。

### 4. ComfyUI 出图链

```text
ai-runner
  -> calls.call('comfy.render', 参数)
      -> comfy.inspectWorkflow
      -> 定位采样器及正/负提示词、尺寸、steps、CFG 节点
      -> 覆盖参数
      -> POST /prompt
      -> 查询 history / 下载 view
      -> images.add(source='comfy')
      -> imageRepository.attachToConversation 或 addToGallery
```

工作流无法定位可写节点时立即返回错误，不把旧参数当作成功结果。

### 5. 外部 Agent 调用链

```text
外部 Agent HTTP :32145
  -> calls/server.js
      -> 校验请求和 caller
      -> listAvailable 或 calls.call
          -> registry/schema/权限检查
          -> 实际业务 handler
      -> 返回统一 JSON 结果
```

外部 Agent 默认只拿 Runtime 工具；`agentWriteEnabled=true` 时才暴露提示词和 Comfy 设置写入工具。

## 五、数据对象之间的关系

```text
ImageAsset（images）
  ├─ GalleryRef（image-repository） 共享画廊引用
  └─ ConversationRef（image-repository）会话引用
       └─ Session.message.imageIds（assistant 会话）
```

`ImageAsset` 保存真实图片和分析结果；GalleryRef/ConversationRef 只保存关系、顺序、显示名和 pending 状态。删除时通过 `referenceCount()` 判断是否可以删除物理文件。

## 六、接口修改影响范围

## 七、V1.4.191 迭代补充

当前实现已增加 `agent.generateTags` 固定子代理工具。它使用 Vision AI 客户端执行一次性请求，接受当前要求、已有 Tag 和参考 Tag，并只返回结构化 `positiveTags`；只有设置 `generateNegativeTags=true` 时才保留 `negativeTags` 字段。ComfyUI 参数已改为从用户设置读取，AI 调用次数使用 `maxComfyCalls`，单次批量出图使用 `batchCount`。外部 Agent HTTP 服务已停用自动启动。

修改一个业务接口时通常需要同时检查：

1. 实现模块的 exports。
2. `preload.js` 的 `contextBridge` 暴露对象。
3. `call-table.js` 的工具 schema 和名称。
4. `calls/index.js` 的 handler/权限分组。
5. `app-view.js` 的调用参数和结果渲染。
6. `assistant.js`/`ai-runner.js` 的 AI 上下文和工具循环。
7. `storage.js` 的键名、迁移逻辑和默认值。

最容易产生连锁问题的接口是 `imageId`、会话引用、`vision.processOne`、`comfy.render` 和 Calls 工具 schema；改动后应优先运行 `npm run check`，再人工验证上传图片、识图、AI 工具调用和 Comfy 出图。
