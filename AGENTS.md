# AI 绘画 Tag 工具箱 · Codex 工作准则（快速迭代模式）

> 与桌面 `更新要求.md` 配套。接到本项目相关任务时，默认按以下规则执行；与用户本次对话的明确指令冲突时，以对话指令为准。

## 1. 工作节奏：小步快跑

- 每次改动只做一件事，改完立即 `git commit`（中文信息，格式 `V版本号：说明`），不要攒改动；
- 不追求一次成功：出 bug 正常，快速迭代靠用户多轮反馈收敛；
- 同一问题修 2~3 次未解决就停下报告用户，不死磕、不反复换方案。

## 2. 测试分级

- **必跑**：`npm run check`（lint + 语法 + 架构护栏 + 单元测试）——快，必须过；
- **默认跳过**：`--uitest` / `--smoke` / `--i18ntest` / `test:regression` 等启动应用的慢测试，由用户人工测试；
- **例外**：改动涉及 AI 推理核心链路（ai-core / ai-job / comfy）时，允许跑一次 `--smoke` 确认主流程没挂。

## 3. 版本号

- **正式版**：`V1.4.3`（一位数修补号 `主.次.修补`）；
- **内部迭代版**：`V1.4.21`、`V1.4.22`…（正式版号后拼接迭代序号）；
- 迭代收敛后提升为正式版（如 `V1.4.3`），版本号在 package.json / VERSION.txt / index.html 三处统一。

## 4. push 时机

- **只有正式版本号变更时**（如 `V1.4.2 → V1.4.3`）才 `git push`；
- 内部迭代版本期间一律不 push，除非用户明确说"推送"。

## 5. 桌面同步（对话轮次制）

- 一轮对话可能内部生成多个小版本，**用户不关心中间过程**；
- **对话结束停下时**，把**最终那个小版本**同步到桌面：
  - 目录名 `C:\Users\admin\Desktop\AI绘画Tag工具箱V1.4.XX`（以现有桌面目录为模板：重命名目录 → 替换 `resources\app.asar` + `app\` 源码 + 版本标识 → exe 改名）；
  - **同步前删除桌面旧版本目录**，保证桌面始终只有一个最新测试版；
  - 必须包含对应版本名的 `.exe`，双击即可运行。

## 6. 完成后

- 报告：最终版本号 / 改了啥 / 跑了哪些检查 / 已知风险；
- **停下等用户人工测试反馈**，不自动续跑、不扩大范围、不自动 push、不自动发 Release；
- 发布（7z + GitHub Release）只在满足发布条件且用户明确要求时执行，见桌面《更新要求.md》第六、七节。
