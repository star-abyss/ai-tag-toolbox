# V1.4.228：双模式批次生成与复刻交付控制

本版重构了对话区的 ComfyUI 生成控制。用户可以设置每轮生成张数和自动迭代上限；自动模式由程序逐轮评价与修订，手动模式每轮结束后释放对话，并只在用户给出点评后继续。

主要变化：

- 一次成功的 ComfyUI 提交计为一轮，每轮支持 1–8 张；失败提交只消耗尝试次数，不占成功轮次。
- 自动模式只用本轮最佳候选驱动下一轮，最终最多横向比较三个轮次最佳项。
- 手动模式支持对单张候选填写意见并直接续跑，不经过主 AI 改写。
- “设为最终结果”可在渲染或评价期间立即结束任务，中断 ComfyUI，并拒绝晚到图片覆盖选择。
- 候选卡按轮次展示，正向与负向 Tag 可分别查看和复制。
- 结果新增 `accepted`、`best_available`、`user_selected` 和 `user_selected_with_issues` 等权威交付状态，并显示残留问题。
- 复刻明确区分参考图已进入工作流与 `text_approximation`；宽高绑定可写时按原图比例生成 64 倍数尺寸，否则保留工作流尺寸。
- 主 AI只接收紧凑生成结果，完整评价留在本地界面；Vision、Tag、评价与主 AI 的 Token 全部计入根任务并按类型汇总。
- 主 AI 保留用户原始要求，不提前识图改写源图事实；Tag 修订增加硬约束、冲突与自然语言否定保护。

验证：`npm run check` 共 137 项通过、0 失败。打包 Electron 使用本地 AI/ComfyUI fixture 完成自动 2 轮 × 2 张、参考图复刻、手动点评续跑、失败提交预算、渲染中最终选择、晚到结果拒绝和普通取消。`1100×800` 与 `1500×950` 截图确认导航、控制栏、候选卡和输入区没有重叠。

已知限制：复杂工作流仍需要用户明确绑定提示词、参考图及可覆盖参数节点。没有参考图绑定时只能文本近似复刻；没有同时启用宽高绑定时沿用工作流固定尺寸。自动评价质量取决于当前视觉模型。
