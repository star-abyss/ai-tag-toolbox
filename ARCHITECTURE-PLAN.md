# AI 绘画 Tag 工具箱：三阶段调整计划

## V1.4.0：国际化里程碑（本地测试版）

- 内置 `zh-CN` / `en-US`，支持系统语言自动检测、手动切换和即时 `localechange` 重绘。
- 语言包使用纯 JSON，主进程负责读取/导入到用户数据目录，禁止执行第三方脚本；缺失 Key 按当前语言 → English → 简体中文回退。
- 界面语言、Tag 英文标准名、Tag 显示覆盖和 AI 输出方向相互独立；错误码保持稳定，错误文案由语言包本地化。
- 本版本先在本地创建测试包，不创建 GitHub Release，待人工验证后再决定是否正式发布。

## 阶段一：运行时一致性（本版本已执行）

- 统一生成页、自由问答、复刻旧路径到 `composeSystem()`。
- `composeSystem('chat')` 保留自由问答的基础规则和问答任务。
- 世界书匹配、编辑器、特殊附录注入改为读取当前世界对象的 `entries`。
- `aiCfg.wb` 暂时保留为旧配置和旧导出的兼容字段，不再作为业务读取来源。
- 修正 localStorage 配额失败时生成对话、问答历史和归档的降级逻辑。
- 大对象图片采用 IndexedDB Blob + `imageIds` 引用持久化；运行时继续提供 Data URL 兼容层，并在写入后校验、缺失时自动修复。

## 阶段二：边界清晰化（本版本已执行低风险部分）

- 新增 `AIClient` HTTP 适配器，保留 `chatComplete()` 兼容入口。
- AI 请求已通过 preload 桥迁移到主进程，Key 使用 Electron `safeStorage` 加密保存。
- 页面已加入 CSP；主窗口禁止任意导航，生产窗口重新启用 sandbox，ComfyUI 推送限制为本机 / 局域网地址。
- 新增 `AIJobController`，统一追踪 AI 请求的排队、运行、成功、失败、超时和取消状态。
- 新增 `PromptCompiler` 统一入口；`composeSystem()` 保留为兼容包装，旧 `buildSys()` 也改走该入口。
- 存储层新增统一字符串/JSON读写、版本标记、失败诊断与明文 Key 防回写保护；图片仓库已统一为 IndexedDB Blob + 引用，保留旧数据恢复与降级兼容。
- 标签关键词提取、相关度匹配和用户 Tag 识别已抽到无 DOM 的 `tag-search.js`，页面与 AI/翻译共用同一入口。
- 标签列表改为批量渐进渲染（首批 160 条，最多 400 条，按需继续加载），降低大标签库首屏 DOM 压力并保留原筛选结果。
- 双向翻译统一生成“输入词 → 候选 Tag → 多中文对照 → 命中证据”参考；AI 翻译提示词按中文 → Tag、Tag → 中文分开编译，避免将 Tag 翻译误当普通句子翻译。
- Tag 输入支持逗号/分号分项、下划线与空格归一化、权重包裹（如 `(blue_hair:1.2)`）精确匹配；多译名参考同时用于展示和 AI 请求。
- 各模块错误统一通过 `app-errors.js` 解释：按鉴权、权限、网络、额度、模型、ComfyUI、存储和数据格式分类，显示用户建议并保留脱敏诊断码。
- 同义词扩充采用独立纯数据 `synonyms.js`：以现有英文标签为基准合并三份中文译名和 SANL 英文别名，经过质量过滤后只追加 `al/hay`，不改搜索算法。
- 标签展示层通过 `tagZhDisplay()` 将主译名与中文别名以 `/` 连接；别名仅改变可见信息，不改变匹配算法。
- 增加 `storageUsage()`，为容量阈值提示和迁移提供数据基础。
- 提示词编译与标签匹配已完成无 DOM 入口；世界书条目已增加统一 Schema 归一化。工作流解析仍保留在 ComfyUI 适配层，避免无收益的过度拆分。
- ES Modules 和局部 TypeScript 延后到需要时逐步引入，不进行一次性全量迁移。

## 阶段三：按规模升级（暂不强制）

- 图片数量、历史体积或搜索分页需求继续上升后，再引入文件系统 / SQLite 索引；当前 IndexedDB Blob 仓库已覆盖本版本的大对象持久化需求。
- 需要密钥隔离时，将 AI 请求迁移到主进程，并通过 `safeStorage` 保存密钥（本版本已完成）。
- 只有当 DOM 状态同步、组件复用或协作测试成为主要瓶颈时，才评估 Svelte / React。

## 验证原则

- 每次收口都保留旧配置迁移和兼容入口。
- 纯逻辑优先用自测脚本验证，避免依赖完整 UI 才能发现规则回归。
- 不因目录分层而增加没有替换价值的抽象层。
