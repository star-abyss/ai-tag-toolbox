# V1.4.220：自动迭代出图与候选评估

本版把主 AI 的绘图流程改为高层任务调度。主 AI 调用 `generation.execute` 或 `generation.resume`；程序状态机负责 Tag 编译、ComfyUI 渲染、候选图评价、定向修订和最终选择。

主要变化：

- 默认自动生成 2–3 张成功候选，失败提交只增加尝试次数，不占成功图片额度。
- 新增固定候选图评估子代理，共用现有识图 API、模型和 Key；支持单图评分、复刻差异分析和多图横向比较。
- 提示词组升级为 V3 六项结构，新增可编辑的候选图评估提示词，并兼容 V1/V2 导入。
- ComfyUI 配置档升级为 V2，可显式声明图生图、Control 参考图和蒙版能力，并绑定参考图、Denoise 与 Control 强度节点。
- 复刻任务会上传当前会话授权原图；没有参考图绑定时明确标记为 `text_approximation`。
- 对话区新增快速 1 张、自动 2–3 张、固定 3 张和自动选择设置；候选卡显示评分、主要问题、实际提示词，并支持手动选择与继续优化。
- 主 AI 不再直接看到 Tag 生成、工作流验证和 ComfyUI 渲染低层工具，旧提示词也会由运行时强制调度协议纠正。
- 调用监视器在记录过大时优先保留最终错误，图片数据、Key、路径和完整工作流继续脱敏。

验证：`npm run check` 必须零失败；另执行一次打包 Electron 的本地 AI/ComfyUI smoke，结果写入 `deployment-verification.json` 和桌面 `build-info.json`。

已知限制：参考图节点不会由程序猜测，复杂工作流需要用户在 ComfyUI 配置页明确绑定。自动评价依赖所选视觉模型的实际识图能力。
