# 更新日志

## V1.4.191

- 修复大尺寸图片上传导致的调用栈溢出，图库、对话图片与 Comfy PNG 工作流共用安全解码链路。
- 兼容 Wildcard、Primitive 与间接 Latent 节点工作流，并用 D 盘 17 份 ComfyUI PNG 验证导入与参数覆盖。
- 删除设置页重复的 ComfyUI 出图开关，修复 API/Comfy 表单保存、模型异步回写、连接结果判断与工作流回填。
- 首次启动可从旧版 Electron 用户目录恢复 API、Comfy 与项目配置，且不会覆盖新版已有的非默认设置。
- 本地 OpenAI 兼容服务优先使用标准 `/v1/models`，保留 Ollama 旧模型接口回退。

## V1.4.190

- 修复 ComfyUI 出图开关被异步状态刷新取消的问题，并恢复跨版本 portable 配置。
- 修复图库和右侧识图图片上传入口，统一使用安全图片存储接口。
- 图片库页面仅保留上传、下载和删除操作，移除导入/导出控件。

## V1.4.189

- 完成独立图片库、会话图片仓库、Vision 单槽临时输入与精简 JSON 调用协议的整合。
- 修复连续对话中的图片重复发送、孤立工具消息和 HTTP 400 风险；增加会话/图片引用、缓存、取消与权限边界保护。
- 收紧 Vision 地址与 data URL 校验、图库删除引用计数、AI 上下文脱敏和兼容调用标记解析。

## V1.4.188

- 新增独立图片库页面、拖拽引用和图库清单导入导出。

## V1.4.184

- 新增图片仓储数据层、旧图片关联迁移与生命周期保护。
- 修正两阶段旧会话迁移和孤立图片物理删除，统一项目可见版本标识。

## V1.4.182

- 识图右侧抽屉动态避开顶部导航栏，改为经典“导航栏 + 右侧面板”布局。

## V1.4.181

- 识图接口拒绝推理字段时自动兼容回退，仍保持非流式最终输出。

## V1.4.180

- 赞助作者按钮改为更鲜艳的橙红色主题，保持统一导航按钮模板。

## V1.4.179

- 搜索按钮缩小并内缩 2px，让搜索栏自身的外框完整露出。

## V1.4.178

- AI 识图关闭流式思维链，更新为详细、精炼且如实包含 NSFW 的描述提示词。

## V1.4.177

- 成人标签使用粉色主题；搜索按钮与搜索栏融为无边框半圆端；重新生成图标调整为不裁切的统一操作按钮。

## V1.4.176

- 搜索按钮改为与输入框融合的右侧半圆弧形；精度选择更紧凑，并用竖线与搜索按钮分隔。
- 复制、修改、重新生成按钮统一为同一横向图标模板。

## V1.4.175

- 重新生成与复制按钮改为同一横向消息操作模板，并增强深色模式下复制/操作按钮的强调效果。

## V1.4.174

- AI 输出下的重新生成与复制按钮改为同一横向操作行并常驻显示；重新生成图标微调位置。
- 提高选中态对比度，修正浅色侧栏选中项强调条的亮度和左边框对齐。

## V1.4.172

- 完善对话操作布局：用户仅显示复制/修改，AI 仅显示复制并提供独立的重新生成按钮。
- 对话消息支持安全的 Markdown、表格、代码块和链接渲染。

## V1.4.171

- 重做对话消息布局：用户消息使用高对比右侧气泡，用户仅保留复制/修改，AI 输出保留复制并将重新生成独立放在输出下方。
- 增加安全 Markdown 渲染，支持标题、段落、列表、引用、表格、行内代码、代码块和链接。

## V1.4.170

- 采用更克制的 Indigo/Blue 主色令牌，提升浅色与深色模式选中态文字对比度，并调浅浅色侧栏选中强调条。

## V1.4.168

- 修正成人标签按钮状态对应关系：开启显示 ● 成人标签：开，关闭显示 ○ 成人标签：关。

## V1.4.167

- 成人标签按钮改用对称的空心/实心圆符号：○成人标签：开 / ●成人标签：关。

## V1.4.166

- 修正标签页面缓存键遗漏数据修订号的问题，避免自定义标签变更后误用旧结果。

## V1.4.165

- 增加搜索按钮并支持回车提交；切换左侧分类时保留搜索框文本，便于再次搜索。
- 子分类按钮改为跟随当前主分类配色；标签列表增加分页缓存、宽松索引按需建立和离屏懒绘制。

## V1.4.155

- 将顶部导航按钮重构为统一状态模板，统一选中/取消选中行为和文案切换，并修复收藏抽屉再次点击关闭。

## V1.4.154

- 修复赞助链接外部浏览器跳转、导航按钮激活态和成人标签颜色，并消除侧边栏选中态重影。

## V1.4.153

- 按 Material 3 / Primer 的颜色角色重做按钮主题，修复深色模式浅底浅字，并改善对话框按钮对比度。

## V1.4.152

- 修复中等宽度窗口下成人标签与搜索栏重叠的问题，搜索/AI 模块在窄桌面窗口独占一行。

## V1.4.151

- 建立统一按钮模板并覆盖主要按钮、图标按钮、分段切换、菜单和标签控件。

## V1.4.150

- 品牌标志贴近左侧边缘，左侧按钮补齐悬停与按下动效。

## V1.4.149

- 缩小左上角品牌图标，恢复版本号显示，并优化中等窗口的顶部单行对齐。

## V1.4.148

- 降低赞助按钮视觉权重，并让成人标签按钮的开启/关闭状态明显区分。

## V1.4.147

- 修复任务过程中的通用事件把同一条提示重复显示两次的问题。

## V1.4.146

- 修复连续对话时把 UI 工具追踪记录误当成 assistant `tool_calls` 发送的问题。

## V1.4.145

- 采用三列顶部导航：搜索居中、左右按钮向搜索区对齐；右侧四个功能按钮统一尺寸并使用分色低饱和样式。

## V1.4.144

- 修复识图抽屉层叠上下文问题，遮罩只变暗背景，识图面板和顶部导航保持清晰。

## V1.4.143

- AI 页面真正隐藏搜索框后显示模式切换器，翻译页保留搜索栏，确保两者宽度一致。

## V1.4.142

- 识图抽屉挂到公共工作区，翻译页等非 AI 页面也能通过导航按钮正常打开。

## V1.4.141

- 重排顶部导航顺序，样式按钮移到搜索栏左侧，搜索与 AI 模块切换器保持同等尺寸。

## V1.4.140

- 识图面板改为顶部导航抽屉入口，移除右侧面板的展开/收起按钮，并移除帮助入口。

## V1.4.139

- 翻译页顶部按钮显示“返回主页”，品牌副标题只保留版本号。

## V1.4.138

- 记住用户上次选择的助手/绘图模式，切换页面或重启后不再强制回到助手模式。

## V1.4.137

- 后续追问不再把绘图返图作为 assistant 图片消息发送，避免接口拒绝。

## V1.4.136

- 为关闭思考参数增加多服务商兼容透传，并在不支持可选参数时自动回退重试。

## V1.4.135

- 识图复制按钮改用与界面显示一致的去重 Tag 列表，避免粘贴时整段重复。

## V1.4.134

- 翻译输入框关闭浏览器拼写检查，英文 Tag 不再显示红色波浪线。

## V1.4.133

- 翻译思考面板在流式更新时保留用户的展开/收起状态，可在思考过程中点击展开。

## V1.4.132

- 翻译 AI 请求改为直接输出模式：提示词禁止思维链、分析草稿和 Markdown，并关闭流式推理回传。

## V1.4.131

### 重构

- 简洁模块化重写，页面外观沿用 V1.4.2 的使用习惯，业务代码不再依赖旧版脚本链。
- 业务代码拆分为 `src/modules/`：`tags.js`、`images.js`、`vision.js`、`vision-service.js`、`translation.js`、`assistant.js`、`ai-runner.js`、`comfy.js`、`prompts.js`、`storage.js` 等。
- 新增 `agent-tools/`（外部 Agent 工具）、`assets/`（标签数据与提示词素材）、`scripts/`（校验脚本）。

### 功能与行为

- Vision 识图一次只处理一个明确的 `imageId`，`mode` 支持 `metadata`、`local` 和 `ai`；页面、主 AI 与外部 Agent 通过同一个 Calls handler 调用。
- AI 页面提供"助手"和"绘图"两种模式；ComfyUI 未连接或工作流无效时，`comfy.render` 不会下发给主 AI。
- Calls 提供动态 capabilities 与 Agent Runtime/Admin 分组，`ai-runner.js` 统一助手/绘图工具循环。
- 外部 Agent 默认只获得 Runtime 工具（Tag、单图 Vision、可用时 Comfy 渲染、提示词/Comfy 设置读取）；开启"允许外部 Agent 修改提示词和 ComfyUI 迭代设置"后才会出现 Admin 工具。
- ComfyUI 工作流提交前按采样器连接关系覆盖固定的正/负向 Tag、尺寸、steps、CFG 等节点；无法定位可写节点时返回明确错误。
- 本地识图与离线翻译模型不随源码入库（`models/` 已忽略），模型缺失时页面仍可打开，本地识图会显示不可用原因。

## V1.4.1

### 新增

- 增加中英文界面与多语言国际化适配。

### 优化

- 优化 API 调用逻辑与长耗时请求处理。
- 完善相关状态提示与错误诊断。

### 注意

- 本地识图与离线翻译仍需随程序保留 `models/` 目录。
