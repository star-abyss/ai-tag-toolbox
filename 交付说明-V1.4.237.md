# V1.4.237：AI 翻译双向对照

AI 翻译完成后，悬停原文或译文可高亮对应内容；点击片段固定高亮，选择文字可反向定位，Esc 取消。原文保持可编辑，复制按钮仍复制完整纯译文，保留空格与换行。继续保留 V1.4.235 的按钮恢复与过期请求防回写逻辑。

V1.4.236 已提交稳定原文 ID 与可选对照协议；本版接入独立 translation-view 子视图并移除 app-view 中的旧翻译交互。程序按 Tag/词分段，AI 返回译文片段对应的 ID，支持重复词、语序变化、一对多与多对一。长文本按分句或块降级，不截断末尾。只有有效关联才展示高亮，无效或缺失的映射保留普通译文，不额外调用 AI 修复。本地翻译保持原样。

修改输入、方向或清空会立即作废旧映射；切页取消未完成请求。译文一律按纯文本渲染，模型返回的 HTML 不会执行。更新了 README、提示词模块说明、主架构文档和部署校验信息；版本号、窗口标题和桌面 EXE 统一为 V1.4.237。

验证：npm run check 共 167 项通过、0 失败，包含 11 个新增对照协议/页面用例；保留原有翻译按钮和取消测试。审查修复了共享片段导致的多余高亮，以及方向切换后的参考词刷新。交付时校验 app/、resources/app/、app.asar 全部 Git 跟踪源码及 unpack 原生依赖，提交与归档哈希记录在桌面 build-info.json。旧 V1.4.235 移至 F:\codex\desktop-backups，桌面仅保留 V1.4.237。

按项目规则未启动慢 UI/烟测、未调用真实 AI 或 ComfyUI。实际对齐质量和耗时取决于当前配置模型，长文本对照会增加输入/输出量；模型若未返回有效映射，页面会提示并显示普通译文。请人工测试英文 Tag、普通句子、重复词、长文滚动和窗口缩放后的对照效果。
