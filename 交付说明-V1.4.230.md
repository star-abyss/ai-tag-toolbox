# V1.4.230：统一生成限制与候选防重

本版把 ComfyUI 配置页和对话页的生成限制统一为同一套设置：每轮批量数量与 ComfyUI `batchCount` 同步，最大调用数量与自动迭代上限同步，两个入口均为 1–10。

同时完成：

- 候选按轮次分组，每轮图片横向排列并支持滚动对比。
- Tag 修订失败、空补丁或修订后提示词没有变化时，立即停止后续生成，不再浪费一次 ComfyUI 调用。
- 持久化恢复任务增加提示词指纹防重，重复提交在 ComfyUI 请求前拦截。
- 对话页第二个限制明确显示为“最多 ComfyUI 调用”，与配置页含义一致。

验证：`npm run check` 共 141 项通过、0 失败。打包 Electron smoke 在 `1100×800` 和 `1500×950` 下通过自动批次、参考图复刻、手动点评续跑、失败提交预算、在途最终选择、晚到结果拒绝、普通取消和重复提示词拦截。

桌面包：`C:\Users\admin\Desktop\AI绘画Tag工具箱V1.4.230`。
