# AI绘画 Tag 工具箱最终架构重构任务书

版本目标：统一 Agent 架构测试版
当前基线：V1.4.192

## 核心设计原则

### 1. 安全性与规范性

本项目不是高安全等级的生产力平台，不引入复杂的企业级权限、沙箱和策略系统。保留的安全与规范只服务于代码清晰和减少错误：主 AI 只能看到固定提示词和固定工具；模块不能读取其他模块的内部状态；模块通过公开接口交换数据；工具输入和输出使用固定结构；错误统一返回明确结果；UI 不直接操作底层文件、AI Client 或内部数据结构。

### 2. 兼容性取舍

本项目没有热更新、增量升级或旧版本配置持续使用的要求。重构时不为旧用户配置、旧提示词、旧会话、旧工具协议、旧外部 Agent 或旧字段保留兼容层。兼容代码只有在不增加复杂度且不影响目标框架时才保留；一旦兼容逻辑使新框架变复杂，直接删除。

### 3. 目标优先于已有实现

已有代码只是参考，不是必须维护的约束。发现旧代码与目标设计冲突，优先重写模块、接口和调用流程。出现报错时，先判断旧实现是否值得修复；如果兼容和重构比重新实现更麻烦，直接重写。最终只要求：目标功能完整、UI 使用方式保持一致或更清晰、模块边界明确、检查通过。

不需要兼容现有提示词。所有提示词都可以根据最终逻辑框架重新编写，提示词内容服从模块接口和子代理协议。

## 一、最终目标

将项目重构为“一个主 AI、若干固定子代理、一组固定工具”的简单模块化应用。

```text
页面
  -> Agent Runtime
      -> 主 AI
          -> 固定工具
              ├─ 查询
              ├─ 会话图片读取
              ├─ 识图子代理
              ├─ 翻译子代理
              ├─ 文生图 Tag 子代理
              └─ ComfyUI 状态/检查/出图
```

主 AI 只能看到提示词和工具定义，不能直接访问文件、存储、图片模块、AI Client 或 ComfyUI Client。固定子代理没有会话上下文，不自由调用工具，只执行固定提示词规定的单一任务。

ComfyUI 工作流内部设计暂时保持现状，不在本次任务中重新设计；本阶段只统一调用入口和参数边界。

## 二、当前已完成设计

### 已完成或基本完成

- `vision.processOne` 已统一 `metadata/local/ai` 三种识图模式。
- Vision 强制使用单个 `imageId`，并增加 `hasBuiltinTags` 状态。
- `agent.generateTags` 已注册并接入 Vision AI。
- Tag 子代理支持当前要求、已有 Tag、参考 Tag 和可选参考图片。
- 默认只返回正向 Tag。
- `generateNegativeTags` 设置已增加，关闭时不返回 `negativeTags`。
- ComfyUI 已拆出 `comfy.status`、`comfy.validateWorkflow`、`comfy.render` 高层工具。
- ComfyUI 主要绘图参数由用户设置读取，AI 不应覆盖。
- `batchCount` 和 `maxComfyCalls` 已分开。
- `batchCount` 已映射到默认工作流和标准 latent batch 节点。
- `maxComfyCalls` 已接入 AI Runner 的 ComfyUI 调用限制。
- 提示词已有内部/外部板块接口和基础 UI。
- 内部提示词已有固定 key、恢复默认、导入、导出和基础 schema 校验。
- 已增加会话图片查询工具 `conversation.listImages`。
- Electron 可启动，V1.4.192 桌面测试包已生成。

## 三、当前仍存在的目标偏差

### 1. 主 AI 仍有助手/绘图模式残留

涉及：

- `src/modules/assistant.js`
- `src/modules/ai-runner.js`
- `src/app-view.js`

仍存在 `mode === 'draw'`、`task === 'comfy'`、绘图模式控件和专用流程。目标是删除模式分支，保留统一 Agent Runtime；ComfyUI 只是主 AI 可调用的一个工具。

### 2. 没有真正统一的 Agent Runtime

当前 AI 请求、工具循环、Vision、翻译、ComfyUI 状态和页面状态分散在多个模块。需要新增或重写统一模块，集中管理：

- 主 AI 请求
- 固定子代理请求
- 工具调用
- 等待状态
- 超时和取消
- 统一错误
- 调用次数
- 结果校验

### 3. 翻译还没有接入固定子代理注册表

`translation.js` 仍是页面和业务模块直接使用的服务，尚未成为与 Vision、Tag 生成相同的固定子代理。

### 4. 主 AI 工具体系仍不完整

已有工具仍混合旧 Runtime/Admin 工具、旧参数和底层接口。需要重写主 AI 的可见工具清单，只保留最终设计需要的高层工具。

### 5. 提示词逻辑仍受旧模式影响

`generate/chat/vision/comfy/quality` 等旧组合仍然存在。目标是保留简单的内部提示词和外部提示词模型，由 Runtime 按固定规则组装。

### 6. UI 仍是一个大型总控制器

`app-view.js` 同时负责标签、会话、图片库、Vision、翻译、提示词、ComfyUI 和 AI 状态。至少应将会话、提示词、设置和 Agent 状态拆出独立视图模块。

## 四、之前为了兼容而产生的不必要内容

以下内容不属于最终设计，应删除，不再维护：

- `comfyIters` 兼容字段及其旧语义。
- `maxIterations`、旧绘图迭代参数映射。
- `prompt`/`negative`/`iterations`/`seed` 等旧 ComfyUI AI schema 字段。
- `mode` 的助手、绘图和旧任务映射。
- `MODE_KEYS` 中的旧模式组合。
- 外部 Agent Runtime/Admin 权限分组。
- `agentWriteEnabled` 设置。
- `calls/server.js` 外部 HTTP Agent 服务。
- `agent-tools/`、旧协议和工具说明文件。
- `migrate.js`、`config-migration.js` 和旧 localStorage 迁移逻辑。
- 旧会话、旧图片集合和旧配置迁移标记。
- `scripts/check.mjs` 中针对外部 Agent 和旧协议的测试。

删除前只需确认当前版本不再依赖这些内容；不需要兼容旧用户数据。

## 五、目标模块树

```text
应用
├─ Host
│  ├─ main
│  ├─ preload
│  └─ module-factory
│
├─ Runtime
│  ├─ agent-runtime
│  ├─ request-manager
│  ├─ status-manager
│  ├─ error-manager
│  └─ usage-limiter
│
├─ Agents
│  ├─ primary-agent
│  └─ fixed-subagents
│     ├─ vision-agent
│     ├─ translation-agent
│     └─ generate-tags-agent
│
├─ Tools
│  ├─ query-tools
│  ├─ conversation-image-tools
│  ├─ vision-tools
│  ├─ translation-tools
│  ├─ tag-tools
│  └─ comfy-tools
│
├─ Domain
│  ├─ conversations
│  ├─ images
│  ├─ tags
│  ├─ prompts
│  ├─ vision
│  ├─ translation
│  └─ comfy
│
├─ Data
│  ├─ storage
│  ├─ settings
│  ├─ sessions
│  ├─ prompts
│  └─ images
│
└─ UI
   ├─ agent-status-view
   ├─ conversation-view
   ├─ tag-view
   ├─ gallery-view
   ├─ prompt-view
   └─ settings-view
```

## 六、统一接口目标

### Runtime

```js
runtime.runPrimary(request)
runtime.runSubAgent(name, request)
runtime.callTool(name, args, context)
runtime.cancel(requestId)
runtime.getStatus(requestId)
```

### 固定子代理

```js
runSubAgent('vision', request)
runSubAgent('translation', request)
runSubAgent('generateTags', request)
```

### ComfyUI

```js
comfy.status()
comfy.validateWorkflow()
comfy.render({ positiveTags, negativeTags? })
```

ComfyUI 的宽高、Steps、CFG、Seed、Sampler、Scheduler、batchCount 全部由设置模块提供。

## 七、重新执行计划

### 阶段 1：重写基础配置和提示词

- 删除旧配置迁移和兼容字段。
- 建立新的 settings 结构。
- 建立内部提示词、外部提示词固定结构。
- 重新编写所有默认提示词。
- 让导入导出只处理新格式。

验收：应用可以读取新配置；提示词可以编辑、恢复、导入、导出。

### 阶段 2：建立 Agent Runtime

- 新建 `agent-runtime.js`。
- 把 AI 请求、超时、取消、等待提示和错误转换集中到这里。
- 把工具调用次数限制集中到这里。
- 主 AI 和固定子代理都由 Runtime 调度。

验收：页面不再直接调用 AI Client；所有 AI 请求经过 Runtime。

### 阶段 3：建立固定子代理注册表

- 注册 Vision 子代理。
- 注册 Translation 子代理。
- 注册 Generate Tags 子代理。
- 每个子代理固定提示词、输入 schema、输出 schema、超时和无思维链设置。
- 子代理无会话上下文，不调用其他工具。

验收：三个子代理都可以通过统一接口执行并返回统一结果。

### 阶段 4：重写主 AI 工具体系

主 AI 只暴露：

- Tag 查询
- 会话图片读取
- 统一识图
- 翻译子代理
- 文生图 Tag 子代理
- ComfyUI 状态
- ComfyUI 工作流检查
- ComfyUI 出图

删除旧底层工具、旧 Admin 工具和外部 Agent 工具暴露。

验收：主 AI 看不到文件、存储和底层模块，只能看到固定工具定义和工具结果。

### 阶段 5：删除助手/绘图模式

- 删除 `mode` 和 `task` 的业务分支。
- 删除绘图模式切换 UI。
- 删除绘图专用 Prompt 组合。
- 保留 ComfyUI 设置和 `comfy.render` 工具。
- 统一会话发送流程。

验收：页面只有一个 AI 对话入口；是否出图由主 AI 是否调用 `comfy.render` 决定。

### 阶段 6：拆分页面模块

- 从 `app-view.js` 拆出会话、提示词、设置、Agent 状态和图片库视图。
- UI 只调用 Runtime API 或公开业务接口。
- 统一显示等待、错误、工具调用和出图结果。

验收：页面不拼接 AI 请求，不直接读取底层存储和模块内部状态。

### 阶段 7：删除外部 Agent 和迁移代码

- 删除 `agent-tools/`。
- 删除 `calls/server.js`。
- 删除旧协议和旧权限测试。
- 删除迁移模块和迁移标记。
- 重写 `scripts/check.mjs`，只测试新架构。

验收：源码中不再存在外部 Agent 启动、旧协议和旧迁移入口。

### 阶段 8：最终验证和打包

- 运行新的 `npm run check`。
- 启动 Electron。
- 验证图片读取、Vision、翻译、Tag 生成、ComfyUI 调用和调用上限。
- 生成新的内部版本桌面目录。
- 确认 exe、源码、资源和版本号一致。

## 八、最终验收标准

- 只有一个主 AI 对话入口。
- 主 AI 只使用固定工具。
- Vision、Translation、Generate Tags 都是固定子代理。
- 文生图 Tag 默认不输出负面 Tag。
- ComfyUI 只接受 Tag，绘图参数由 UI 设置提供。
- `batchCount` 和 `maxComfyCalls` 含义完全分离。
- 所有 AI 请求经过统一 Runtime。
- 所有请求都有统一等待、超时、取消和错误反馈。
- 内部提示词和外部提示词独立管理。
- 不再保留旧配置、旧模式、旧协议和外部 Agent 兼容代码。
- 检查脚本通过，桌面版本可以启动并完成基本人工验证。
