# V1.4.199 UI 与翻译匹配优化

## 本轮变更

- 角色详情提供“复制角色词”“复制角色与外貌词”“复制角色与所选特征”三个复制按钮；复制按钮位于加入按钮左侧并使用醒目的青绿色配色。
- “复制角色与外貌词”只加入角色身份、作品词和全部通用外貌特征，不带学校、阵营等隐藏专用词。
- 普通 Tag 搜索命中角色名时显示“查看角色”快捷按钮，点击后进入角色库并带入角色 ID 查询。
- 角色库从“人物与角色”下方移为侧栏独立入口，位置在“全部标签”和“质量词”之间，并使用独立边框颜色。
- 翻译模块的站内 Tag 参考只接受精确英文、精确中文译名或已确认别名；移除包含式短词匹配，避免“手套”误命中“手/握手”等 Tag。参考按钮标题会显示匹配方式。

## 验证

- `npm run check`：49 项测试通过，0 失败。
- 未运行启动应用的慢 UI、smoke 或 ComfyUI 出图测试；桌面包留给人工测试。
- 版本标识：package.json、VERSION.txt、index.html、Electron 标题均为 V1.4.199。

## 已知边界

- 角色数据仍为 V1.4.198 使用的公开上游快照，未下载图片。
- 翻译参考变得更保守；未精确匹配的自然语言片段交给翻译模型处理，不再用模糊站内 Tag 猜测。
