# V1.4.202 角色查询调度

- 普通 `tags.search` 结果统一增加 `attachedData` 字段，普通 Tag 默认为空对象。
- 命中角色名时，`attachedData` 返回角色 ID、姓名/作品身份 Tag 和外貌 Tag；角色专用词仍不进入普通 Tag 词条。
- 主 AI 遇到角色名称或不确定 Tag 时先调用搜索，只负责查询、确认和调度。
- `agent.generateTags` 继续按确认的 `characterIds` 自动展开角色资料，由文生图子代理筛选外貌 Tag。
- 修复统一 Runtime 漏掉 `characters.search` 的问题，主 AI 工具 schema 恢复为 9 个。
- `npm run check`：53 项通过，0 失败。
