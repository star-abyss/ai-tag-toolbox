# V1.4.213：AI 调用监视器与状态栏修复

## 使用方式

对话区 ComfyUI 调试按钮旁新增「调用监视器」。API 页的「固定 Agent Runtime」区域也能打开。

- 自动刷新最近的主 AI、工具和子代理调用，可按整次请求筛选。
- 点击一条调用，查看调用输入、处理后的输出、错误、运行事件，以及每次 API 的实际请求和返回。
- API 请求包含系统提示词、历史消息、工具定义、模型和参数；返回包含解析前文本、接口返回的 Token 用量和耗时。
- 文生图子代理输出无效 JSON 时，原文仍能在监视器中检查。
- 「复制 JSON」和「导出日志」使用当前筛选；「清空记录」需确认，清除日志但不清除对话或图片。

## 本地日志

日志单独保存到 `%APPDATA%\ai-tag-toolbox-rewrite\debug\ai-calls.json`，外部调试可直接读取该文件，也可使用导出的 JSON。

密钥、图片内容和已识别的本地路径在写入前隐藏。最多保留 200 条调用、约 12 MiB；每条约 1 MiB，长文本约 128 KiB，超限内容会明确标记截断。写入采用短延迟合并和临时文件替换。正常重启可恢复日志，未完成记录显示为已中断。

监视器从此版本开始记录，无法补回旧版未保存的原始接口内容。流式接口保存合并后的文本、工具调用和用量，不保存逐个网络分片；供应商没有返回 Token 用量时显示「未返回」。日志不会发给 AI，也不加入对话历史。

## 状态栏修复

截图中的「【进度 步骤…】」由主 AI 提示词要求生成。过去调用失败时，页面把累计的 AI 回复 `result.text` 当作状态填进顶部，导致它与 ComfyUI 控件挤在一起。

顶部现在只使用简短的请求状态和工具名称；ComfyUI 的长说明放在悬停提示中。详细进度仍保留在消息和日志中。按钮不再随状态文字压缩，空间不足时状态行换行。

## 验证

- `npm run check`：80 项测试通过，0 失败。
- 覆盖实际本地 HTTP 流式请求、多轮主 AI、翻译/识图/Tag 子代理、无效 JSON、超时、文件恢复、脱敏、容量限制、筛选与导出、截图问题的 DOM 复现。
- 已启动打包后的 Electron V1.4.213，在 1100×800 窗口下通过本地模拟 API 复现子代理无效输出；确认顶部控件不重叠、监视器可展开原始返回、日志可导出并写入本地文件。
- 内部测试版本，仅本地提交，不 push、不发 Release。
