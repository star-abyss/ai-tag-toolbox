# V1.4.31 · thinking 模式 HTTP 400 修复

报错原因：AI 返回的推理内容在工具调用消息、会话历史和请求序列化时被丢弃。DeepSeek 等模型在 thinking 模式且请求携带 tools 时，要求回传所有历史 assistant 消息的 reasoning_content，否则返回 HTTP 400。

本版逐轮保留原始推理内容并随后续请求发送，覆盖工具调用、最终回答、流式/非流式输出、会话保存与恢复、会话导入。普通响应没有推理字段时不补造推理；显式空推理字段保留为空字符串。

验证：必跑 npm run check；新增 tests/ai-reasoning.test.cjs 的 8 项回归。模拟接口已在修复前复现原始 400。最终检查结果及桌面包源码、asar、exe 校验信息记录在桌面根目录 build-info.json。

人工测试：新建对话，用原平台/模型发送一个会触发工具调用的问题，完成后继续提问，重开程序后再继续同一对话。

已知限制：旧版已丢弃的逐轮推理无法恢复，旧对话若继续返回同样的 400，请新建对话。尚未使用反馈用户的真实 API 验证；默认跳过启动应用的慢测试。

协议参考：https://api-docs.deepseek.com/guides/thinking_mode

本版为内部测试版，仅本地提交和桌面同步，不推送、不发布 Release。
