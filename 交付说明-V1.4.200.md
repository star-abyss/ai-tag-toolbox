# V1.4.200 角色库计数与性能方案

本轮将角色库收录数量显示在左侧入口，显示为当前运行时角色总数 `34,122`。数量来自轻量 manifest，不会为了渲染侧栏而解析完整角色表。

性能分析已记录在 `docs/性能分析与优化方案-V1.4.200.md`。当前测量显示：标签库冷加载约 148.5 ms，角色库首次完整索引约 155.3 ms，角色首次搜索约 33.4 ms。主要卡顿来自同步 JSON 解析、全表搜索和批量 DOM 重绘。

本轮先完成低风险的轻量计数读取；性能优化方案分为四阶段：预计算搜索索引和减少 DOM 更新、角色库异步加载、角色数据分片、性能基准回归。异步加载属于后续独立改动，避免在本轮改变现有同步接口。

验证：`npm run check` 50 项通过，0 失败。未启动慢 UI、smoke 或 ComfyUI 出图测试；等待人工测试反馈。
