# V1.4.201 性能阶段 A

- 左侧角色库入口显示收录数量 `34122`，数量只读取轻量 manifest。
- 角色首次加载使用精简角色名索引，避免克隆完整 Tag 视图。
- 普通 Tag 搜索输入不再反复重建侧栏分类 DOM。
- 性能实测：标签库约 133.5 ms，角色完整索引约 139.4 ms，角色搜索约 21.1 ms。
- 完整 `npm run check`：52 项通过，0 失败。

下一步是阶段 B 的角色异步加载。本轮不改变现有同步接口，未运行慢 UI、smoke 或 ComfyUI 出图测试。
