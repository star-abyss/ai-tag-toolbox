# AI绘画 Tag 工具箱：架构与功能地图（V1.4.191）

> 用途：给后续 AI 或维护者快速了解项目。本文依据 V1.4.191 实际源码整理；文件路径均相对于 `app/`，定位问题时先看“入口链路”和对应模块。

## 1. 产品边界与运行方式

这是一个 Electron 本地桌面应用：主进程负责窗口、旧版本迁移和外部链接；渲染器负责页面交互；业务模块在 preload 中实例化并通过 `contextBridge` 暴露。应用无需后端服务器，设置、会话、标签状态和图片索引写入用户目录 JSON，原图写入用户目录图片目录。

启动入口：`main.js` -> 创建 BrowserWindow -> 加载 `src/index.html`。窗口使用 `contextIsolation: true`、`nodeIntegration: false`。`preload.js` 读取素材、创建所有业务实例，然后把安全 API 挂到 `window.aiTag`（页面不直接接触 Node、fs 或 localStorage）。

## 2. 总体调用链

```text
Electron main.js
  └─ preload.js
      ├─ storage -> tags / images / vision / prompts / comfy
      ├─ assistant (AI 服务 + 会话 + imageRepository + calls)
      ├─ translation (本地翻译优先，AI 兜底)
      └─ calls server (http://127.0.0.1:32145，外部 Agent 可选)
          └─ src/app.js -> app-view.js -> DOM/UI
```

页面入口：`src/app.js` 负责拿到 preload API、初始化路由和视图；`src/app-view.js` 是主要 UI 控制器，包含标签页、AI 会话、图片库、识图、翻译、设置、ComfyUI 配置及事件绑定；`src/app.css` 负责样式；`src/index.html` 定义页面骨架。

## 3. 模块设计与接口

### 3.1 存储与迁移

`src/modules/storage.js`：`createStorage({ prefix, filePath })` 提供 `get/set/remove/has/clear/export/import` 等轻量键值接口，文件适配器负责 JSON 原子写入和容错。所有业务模块通过注入的 storage 持久化，不自行操作 localStorage。

`main.js` 的 `migrateLegacyUserData()` 处理旧 Electron userData、旧 safeStorage 加密 API Key 和 Local Storage；`src/migrate.js` 把旧标签状态、设置、会话迁移到新键名；`config-migration.js` 定义旧键白名单、迁移标记和合并规则。故障时保留备份 `rewrite-storage.before-legacy-migration.json`。

### 3.2 标签目录

`src/modules/tags.js`：`createTags({ sources, storage })`。数据来自 `assets/数据资产/标签/*.js`，支持数组行和对象行；建立英文 tag、中文名、别名、分类、子分类、成人标记的索引。核心能力包括 `listCategories/listSubcategories/search/get/select/toggle/clear/getSelected/addCustom/updateCustom/removeCustom`（具体暴露名以文件末尾 exports 为准）。选择状态持久化，搜索支持普通和精确模式，NSFW 可由 UI 开关过滤。

### 3.3 图片与图片引用

`src/modules/images.js`：`createImages({ analyzer, storage, imageDir })` 管理图片资产对象、缩略图、顺序、PNG 元数据和分析结果。输入可以是 data URL、Buffer、Uint8Array 或 ArrayBuffer；模块生成稳定 `img_*` ID，原图落盘，`analyze(id/options)` 调用注入的 Vision analyzer。

`src/modules/image-repository.js`：`createImageRepository({ images, storage })` 管理引用关系：画廊引用、会话引用、pending/sent 状态、拖拽关联、删除和孤儿回收。关键接口：`listGallery/listConversation/attachToConversation/addToGallery/removeFromGallery/promoteConversationImages/deleteSession/referenceCount/exportGalleryManifest/importGalleryManifest`。删除物理文件前必须确认引用计数为零；会话标题只写会话引用，避免改名影响共享画廊。

### 3.4 Vision 识图

`src/modules/vision.js`：`createVision({ modelsDir })` 适配本地 WD EVA02/WD tagger；读取 `tags-canary.json`，对图片解码、推理并返回带置信度、类别和建议 tag 的结果。模型缺失不阻塞主页面，只返回明确 unavailable 原因。

`src/modules/vision-service.js`：`createVisionService({ vision, ai, images, tempStore })` 统一入口 `processOne({ imageId, mode, prompt, ... })`，mode 为 `metadata`、`local`、`ai`，一次只处理一个明确 imageId；负责图片引用安全校验、临时文件、事件进度和结果格式化。页面、助手和外部 Agent 都调用 `vision.processOne`，不要绕过此层直接调用模型。

### 3.5 AI 助手与绘图任务

`src/modules/assistant.js`：`createAssistant({ tags, images, vision, comfy, storage, promptDir })` 组合 `AiService`、会话存储、提示词和图片仓库。AI 配置包括 OpenAI 兼容 `base/model/key/temperature/timeout`，Vision 可继承主 AI 配置或单独配置。会话消息保存 role/content/imageIds/tool calls/thinking，支持中止、恢复、导入导出。

`src/modules/ai-runner.js`：`createAiRunner` 为“助手”和“绘图”共用工具循环：刷新 capabilities -> 组装系统提示和 OpenAI tools -> 请求模型 -> 执行工具调用 -> 追加 tool 结果 -> 继续请求，直到文本完成、取消或错误。绘图模式可选 Comfy 渲染；勾选实际出图且工作流有效才执行 `comfy.render`。识图结果可在绘图链路中自动补充。

提示词文件位于 `assets/提示词素材/`：主提示词、生成任务、自由对话、识图默认提示词、Comfy 协议、质量前缀、附录和来源说明。`src/modules/prompts.js` 负责读取、覆盖、启用/禁用、自定义提示词以及导入导出。

### 3.6 Calls 工具协议与外部 Agent

`src/modules/call-table.js` 定义工具名称、参数 schema、权限和执行器；`src/modules/calls/index.js` 的 `createCalls` 注册实际 handler，并提供 `listAvailable/refreshCapabilities/has/resolve/call/openAiTools`。工具分 Runtime 和 Admin：Runtime 默认包含标签、单图 Vision、提示词读取、Comfy 渲染（可用时）；Admin 的提示词/Comfy 设置写入能力必须由 `agentWriteEnabled` 显式打开。

`src/modules/call-protocol.js` 兼容解析模型返回的 JSON 调用、旧格式调用、图片引用和单轮调用限制；`execute` 负责校验、权限、调用和摘要。`src/modules/calls/server.js` 启动本地 HTTP Agent 服务（默认端口 32145），请求进入 `calls.call`，caller 固定为 `external-agent`，写权限由 preload 的设置回调决定。协议和示例见 `agent-tools/protocol.md`、`agent-tools/README.md`。

### 3.7 ComfyUI

`src/modules/comfy.js`：`createComfy({ base })` 封装 `/system_stats`、`/prompt`、`/history`、`/view` 等接口，支持 JSON 工作流和 PNG 内嵌 workflow 提取。提交前根据采样器连接关系定位正/负提示词、尺寸、steps、CFG 等节点并覆盖；找不到可写节点返回明确错误，不假报成功。渲染结果下载后交给 `images.add`，来源标记为 `comfy`，再由 imageRepository 关联会话或画廊。

### 3.8 翻译

`src/modules/translation.js`：`createTranslation({ tags, runner, ai })` 判断中英文方向，拆分逗号/换行 tag，先做本地参考匹配（同义词、中文名、精确英文），本地 HuggingFace 模型可用时优先翻译，否则用 AI；输出原文、译文、匹配 tag 和可复制的 tag 文本。模型候选目录由 `preload.js` 搜索 `models/translation`。

## 4. UI 功能与预期行为

标签页：分类/子分类浏览、关键词搜索、点击选择、已选 tag 管理、复制全部、收藏保存、添加自定义 tag、NSFW 过滤。拖入图片可切换到识图或关联上下文。

AI 页：助手/绘图两种模式；新建、切换、清空、删除会话；消息支持文本、thinking、tool 调用和图片；上传图片、拖拽图片、pending 图片引用、停止生成；绘图模式控制是否渲染、迭代次数和 Comfy 参数。

图片库：上传、缩略图网格/单列、尺寸和新旧排序、搜索、选择、重命名、下载、删除、导出/导入 gallery manifest。共享画廊图片可被多个会话引用，只有零引用时才物理删除。

识图/翻译/设置：本地识图显示模型可用性和进度；AI 描述使用 Vision Service；翻译支持方向切换和 Tag 参考；设置页维护主 AI、Vision、Comfy、超时、严格工具协议、外部 Agent 写权限、提示词集合和世界书/预设导入导出。

## 5. 关键数据流与排错定位

1. 页面无响应：先看 `src/app.js`、`src/app-view.js`，再检查 preload 是否成功创建 modules；启动日志在 `debug.log`。
2. 设置丢失：检查 `storage.js`、`preload.js` 的 `storagePath` 和 `main.js` 迁移标记；用户数据通常在 `%APPDATA%/ai-tag-toolbox-rewrite/rewrite-storage.json`。
3. 标签搜索/选择错误：看 `tags.js` 的索引和持久化，再看 app-view 的 `searchPrecision`、`selected` 事件。
4. 图片无法显示/误删：检查 `images.js` 的 bytes/dataUrl 处理和 `image-repository.js` 的 `referenceCount`、会话/画廊引用。
5. 识图失败：先确认 `models/tags-canary.json` 和 `vision.js` 可用性，再看 `vision-service.js` 的 imageId 安全校验与 mode。
6. AI 工具循环异常：看 `assistant.js` 的 AiService 请求、`ai-runner.js` 的循环状态，再看 `call-protocol.js` 解析和 `calls/index.js` handler。
7. Comfy 出图失败：先调用连接测试，再检查工作流 JSON、采样器节点定位和 `comfy.js` 的覆盖错误；确认未连接时绘图模式不会发送 render。
8. 外部 Agent 失败：检查端口 32145、`agent-tools/protocol.md` 请求格式、工具 capabilities，以及设置中的 `agentWriteEnabled`。

## 6. 构建、检查与已知边界

在 `app/` 执行 `npm run check`，检查语法、架构护栏和单元测试；`npm run dev` 启动 Electron。慢速 UI/smoke 测试默认由人工执行。模型和 Electron 依赖缺失时，标签库、普通页面和配置仍应打开，但本地识图/翻译/Comfy 会显示不可用原因。修改接口时应同步 `preload.js` 暴露层、`call-table.js` schema、UI 调用点和本文件。

