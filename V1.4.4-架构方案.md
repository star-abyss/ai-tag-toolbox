# AI 绘画 Tag 工具箱 V1.4.4 架构方案

> 这是 V1.4.4 的设计基线。V1.4.91 已按本方案完成后续收敛，并修复识图模型兼容问题：页面和主 AI 只使用 `assistant.run` 的 assistant/draw 两种模式；Vision 统一为 `vision.processOne`；Agent 工具按 Runtime/Admin 分组；旧文本 Comfy 协议和旧四模式入口已删除。

## 目标

V1.4.4 将应用内主 AI、识图 AI、外部 Agent 和页面操作统一到同一套业务调用接口。界面层只负责收集输入和显示结果，API 客户端只负责与模型通信，ComfyUI 连接器只负责与 ComfyUI 对接，具体能力由独立的 Calls 调用层编排。

## 总体分层

```text
应用内主 AI / 外部 Agent / UI
              ↓
      Calls Tool Registry
              ↓
 Tag / Vision / ComfyUI / Prompt tools
              ↓
 tags.js / images.js / vision.js / comfy.js / prompts.js
```

## 双 API

- `primary`：主对话 API，负责对话、推理、任务规划和最终输出。
- `vision`：识图 API，负责图片理解、绘图 Tag、姿势和局部分析。
- 两者使用同一个 API 客户端实现，只保存两份配置和客户端实例。
- `vision.inheritPrimary=true` 时，识图 API 使用主 API 的连接配置；取消后独立填写地址、Key、模型。
- 主 AI 收到图片后可以直接分析，也可以自主调用 `vision.processOne`；调用模块不会强制每次转发。

## Calls 工具域

### Tag 查询

`tags.search`

### 识图调用

`vision.processOne({ imageId, mode, model, instruction })`

一次只处理一个明确的 `imageId`，`mode` 为 `metadata`、`local` 或 `ai`，不接受 `imageIds` 数组。

本地识图只提供常见绘图 Tag 候选，可能漏掉细节、误判相似内容、识别过多或无法判断复杂关系，只能作为辅助参考。

### ComfyUI 调用

`comfy.render`、`comfy.history`、`comfy.getImage`

`comfy.js` 不负责判断意图和迭代；主 AI/Assistant 通过 Calls 决定是否再次调用 `comfy.render`。

### 提示词调用

`prompts.list`、`prompts.read`、`prompts.compose`、`prompts.create`、`prompts.delete`、`prompts.enable`、`prompts.reset`

## 提示词保护

- 内置默认文本永久保留，不允许修改或删除。
- 当前覆盖值单独保存；主提示词和识图主提示词允许修改覆盖值，不允许删除。
- 内置提示词允许停用；恢复默认会清除覆盖值并恢复启用。
- 自定义提示词可以新建、修改、停用和删除。

## 外部 Agent

`agent-tools/` 提供稳定的业务名称、参数和返回值说明，不要求 Agent 阅读源码猜接口。首版提供 JSON-RPC 本地入口，后续可在同一 Registry 上增加 MCP 适配器。

默认外部 Agent 为只读，只返回 Runtime 工具；开启 `agentWriteEnabled` 后才返回 Admin 工具。权限是防呆开关，不引入复杂账户系统。

Runtime 工具包括 `tags.search`、`vision.processOne`、可用时的 `comfy.render`、`prompts.list`、`prompts.read` 和 `settings.comfy.get`。Admin 工具包括 `settings.comfy.update`、`settings.comfy.setIterations` 以及提示词的 `create/update/delete/enable/reset`。

## 图片返回

图片统一以 `artifact` 返回，至少包含 `id`、文件名、路径、MIME、尺寸和提示词元数据。需要查看图片时使用 `comfy.getImage` 或对应 artifact 查询，不在每次调用中重复塞入完整 Base64。

## 迁移原则

旧的文本指令解析不再作为运行时入口。所有能力先进入 Calls，再由统一 Runner、UI 或 Agent 适配器使用；旧会话仅在恢复时一次性映射为 assistant/draw。
