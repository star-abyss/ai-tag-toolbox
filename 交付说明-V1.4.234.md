# V1.4.234：监视器滚动与角色参考传递

本版包含 V1.4.233 的调用监视器滚动修复：弹窗高度受窗口约束，列表独立滚动，记录不再被压扁，长 JSON 保留自己的滚动区域并放大字号。

角色资料继续作为参考：主 AI 传用户要求及角色 ID，程序将姓名、出处、外貌和专用词交给 Tag 子代理，并补充给单图评价与横向比较子代理。编译、修订、评价共用一套优先规则：用户要求优先；原图是修改前的观察；角色资料按人数、景别、可见范围和构图选用。允许精简、改写或删除参考 Tag，上半身可删除靴子，多人可减少外貌。程序不锁定或自动补回角色外貌 Tag。

修订协议将 preserve 的含义改为“仅本次补丁保留”，与现有程序行为一致。已保存的自定义提示词会在实际调用时附加新规则，无需用户重置。

## 本次天子实测诊断

读取的是 2026-09-08 12:22:52–12:24:48 的恢复任务，根请求为 `generation_character_54f7be11-278d-4591-b90b-832c98514484`。

| 环节 | 证据 | 结论 |
| --- | --- | --- |
| 角色库 → Tag 编译 | 含 hinanawi tenshi、touhou、blue hair、red eyes、long hair、very long hair、hat | 资料已送达，角色数据未漏传 |
| 首次评价 | 仅获角色 ID 和原图蓝图，没有完整角色资料；把蓝发红眼判为硬错误，声称目标应有金发、尖耳、光环 | 原图外貌被误当成目标角色资料 |
| 修订 | 移除 blue hair、red eyes、hat，添加 blonde hair、yellow halo、angel wings 等原图特征 | 错误评价把后续生成带偏 |
| ComfyUI 实际历史 | 正向节点 6 第一轮含蓝发红眼，后两轮改为金发；三轮均无参考图输入节点 | 提示词实际写入成功，属于文本近似复刻，不是图生图强度问题 |

ComfyUI 三个实际任务编号：`00b8720a-3ca3-432a-b944-9b63dfe06604`、`e7f1fa7f-4eff-440c-9506-d73aebf974bc`、`7b25fbdd-10a5-4d11-b441-77ea04b4890b`。日志 `changedBindings: []` 不能独立证明提示词未写入：旧自动绑定路径未完整记录此字段。

## 验证与边界

`npm run check`：151 项通过，0 失败。覆盖完整角色资料流入评价、普通复刻、允许按上半身景别省略及删除外貌 Tag、横向比较和无新增外貌锁定。

已运行一次打包烟测：75 条记录的列表高度 514 px、可滚动内容高度 4186 px、最小记录高度 40.5 px，顶部操作栏不随列表滚动；长 JSON 独立滚动。评价子代理与 Tag 子代理接收到同一份角色资料。原有角色选择、自动迭代、手动点评、复刻、重复提示词拦截、最终选择中断和取消均通过。最终证据保存在桌面根目录 `build-info.json`。真实模型的修订与评价仍具有不确定性，需要用真实 API 人工复测；本版修正调用上下文和规则，不承诺每次生图都准确。
