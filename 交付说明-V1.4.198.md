# V1.4.198 本地角色库

## 本轮变更

- 主页“人物与角色”下面新增“角色库”，替代旧角色名浏览入口。
- 33,599 条带特征角色资料，加上 523 条旧角色名兼容，共 34,122 条可查询记录。
- 227 个已整理的通用词补入原标签库，含中文及分类；804 个专用/待复核词集中保存，只随角色资料展开，常规词库不加载它们。
- 角色资料复用原有通用 Tag ID；同一阵营、学校制服等专用词只保存一份，角色按 ID 引用。
- 支持中英文角色名、现有别名、作品/来源检索，沿用顶部搜索框和搜索精度；角色列表每页 50 条。
- 无图片的角色详情显示身份词和可选参考特征，支持仅加入角色、加入角色与所选特征、复制。
- 角色组合保存在独立选择记录中，可从底栏移除、清空和与普通 Tag 一起复制；取消角色不取消手动选择的普通 Tag。
- 新增 AI 工具 `characters.search`。生成工具接收 `characterIds`，程序自动展开带角色归属的参考资料；用户明确要求优先。

## 数据来源

当前内置 Laxhar/noob-wiki 于 2024-11-14 的公开上游快照，版本 929c972dcc8aeecde42b7cd8931afe82cd864424。
这是 AniMadex 作者引用的上游数据，不是 AniMadex 官网当前完整导出。不含角色缩略图、原图或 LoRA 文件，运行时角色检索不访问网络。AI 推理仍使用你原有配置。

通用/专用判定记录见 `app/assets/数据资产/角色/word-decisions.json`；来源和数量见同目录 `manifest.json`。原始 CSV 和清洗分析仍在桌面 `AniMadex角色数据-20260906`。

## 已验证

- `npm run check`：44 项测试通过，0 失败，架构检查及既有回归检查通过。
- 对所有 34,122 条运行时角色记录检查了 AI 输出 schema，全部通用及专用引用可解析。
- 核对学校/阵营等新增专用词不加入普通 Tag 索引。
- 核对独立选择恢复、成人开关、语言切换、括号转义、复制去重、空结果和数据加载错误。
- 核对角色搜索输入后立即切到 AI 的延迟回调，不会改写普通 Tag 查询；返回各页面时分别恢复搜索内容。
- 打包时比较归档内全部项目文件与工作区 SHA-256，并确认原生依赖处于解包目录。
- 未运行启动应用的慢 UI、smoke 或 ComfyUI 出图测试；没有 push 或发布 Release。
- 静态本地 HTML 的浏览器预览被浏览器安全策略拦截，没有完成截图视觉验收。DOM 行为验证通过，最终界面效果需人工测试。

## 人工验证重点

1. 打开角色库，搜索“初音未来”，选择角色并勾选两项参考特征，加入后复制底栏内容。
2. 搜索作品英文名，例如 `vocaloid` 或 `touhou`，选择作品筛选，切换分页和搜索精度。
3. 普通标签页搜索“浅绿色头发”，应能找到新通用词；搜索 `kibina high school uniform` 不应出现新增的专用词。
4. 在角色详情中切换语言和成人开关，核对勾选状态、可见词和底栏内容。
5. 在 AI 页要求查询一个具体角色，核对 `characters.search` 结果，再要求生成带指定服装的角色，确认用户服装要求优先。

## 已知限制

- 新来源中的角色和作品没有完整中文译名；优先复用现有角色中文名与别名，其他条目显示英文。作品筛选当前以来源英文标识为主。
- 804 个隐藏词中有 606 个属于低频或语义待复核项，保留原文并标记待复核。没有将它们猜测成普通词库同义词。
- 来源中可能有多个发色、眼色、服装形态，作为可选候选保留，不自动强制组合。
- 角色组合使用独立选择保存；既有“收藏组合”仍只保存普通 Tag，本轮没有扩展旧收藏格式。
- 官网当前完整快照仍需要账号导出令牌。后续可更新数据文件，当前实现没有把上游行数冒充官网数量。

任务计划书：`app/docs/superpowers/plans/2026-09-06-local-characters.md`。
设计依据：`app/docs/superpowers/specs/2026-09-06-local-characters-design.md`。
