# V1.4.238：关闭翻译思维链并修复双向对照

翻译子代理继续使用非流式请求，并明确发送 `reasoning_effort: none`、`enable_thinking: false` 和 `thinking: { type: "disabled" }`。即使兼容接口错误返回 reasoning，固定翻译子代理也不再向页面发送增量或思考事件；翻译页的思维链面板和样式已移除。

本轮复盘了用户真实调用记录：AI 返回了完整的 `targetSegments`，但漏写了片段之间的逗号，旧校验因此整体降级为普通译文。现在程序会在模型片段之间只补回连续的空格/标点作为无关联片段，保留有效的 sourceIds；如果缺失的是有意义文字、顺序不一致或修复后超过片段上限，仍安全降级为普通译文。

原文 textarea 选区和译文片段选区均可双向高亮。selectionchange 先解析译文容器中的真实选区，再考虑原文 textarea 的焦点，解决拖选译文时焦点仍停在原文输入框造成的误判。真实 Edge headless 烟测确认两侧高亮都有可见背景色；复制仍为完整纯文本。

验证：`npm run check` 最终通过项数以本轮命令输出为准；另有真实请求重放、翻译页面专项测试和 Edge 浏览器高亮烟测。未调用外部真实 AI，真实请求只使用已保存的脱敏监视器返回重放。版本、窗口标题、桌面目录和 EXE 统一为 V1.4.238；旧 V1.4.237 将移至备份，桌面只保留最终包。

已知边界：对照的语义关系仍由配置模型提供；程序只修复明确可识别的标点/空白缺口，不猜测缺失语义。模型返回大量片段时仍受 512 段上限限制。
