# V1.4.232：角色选择与原任务续跑

角色查询有多个候选时，对话中显示选择卡片，包含角色名称、作品、英文标识和重新搜索入口。点击后程序直接使用原任务编号恢复，不要求主 AI 再次抄写编号；等待用户选择期间结束本轮主 AI 调用，释放对话输入。

已确认的角色 ID 优先于重复角色名或相同作品名查询。唯一命中和用户选择都读取完整角色资料后再编译 Tag，包含身份、外貌和角色专用词。失效 ID 可重新选择替换，多角色任务逐项确认，旧选择和连续点击不能重复启动任务。用户的选择、恢复工具结果及用量保存在对话和调用监视器中。

检查：`npm run check` 148 项通过，0 失败；包括角色查询优先级、外貌资料、失效 ID 替换、多角色恢复、过期选择、跨会话限制、本地重新搜索和按钮防重。已运行一次打包后烟测：角色选择及本地搜索成功，同一任务编号恢复，主 AI 只调用一次，完整外貌进入 Tag 子代理，产生 1 张候选图。原有自动迭代、手动点评、复刻、重复提示词拦截、在途最终选择和取消流程同时通过。证据见桌面根目录 `build-info.json`。

边界：每次搜索显示前 10 个候选，可输入更具体的名称缩小范围。本次交互只处理角色确认。实际生图质量需人工测试。
