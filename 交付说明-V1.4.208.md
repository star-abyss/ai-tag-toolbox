# V1.4.208 ComfyUI 配置工作区

- AI 工作台新增独立 ComfyUI 配置页；对话区“调试”按钮直接跳转。
- API 页不再保存或重置 ComfyUI 参数。
- 旧版单工作流自动迁移为“默认工作流”配置档。
- 工作流导入后分析采样器、提示词、尺寸、批量和输出节点；多采样器可选择主采样器。
- 默认只覆盖正向/负向提示词，尺寸、步数、CFG、Seed、采样器、Scheduler、批量数量和模型默认由工作流决定。
- 过期节点绑定在提交前报错，避免静默修改错误节点。
- ComfyUI 状态显示版本、设备和队列；`/object_info` 按地址缓存 5 分钟。
- 已针对本机 ComfyUI 0.34.0 和当前 Anima 工作流完成只读分析：12 节点、KSamplerAdvanced、正负提示词、EmptyLatentImage 和 SaveImage 均可识别。
- `npm run check`：67 项通过，0 失败。
