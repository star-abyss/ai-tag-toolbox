# AI 绘画 Tag 工具箱 V1.4.3 更新说明

GitHub 上一个版本是 V1.4.191（2026-09-05 发布的临时修 BUG 版），重点是连续对话图片、搜索筛选、关闭翻译/识图思维链和图片库。V1.4.3 在此基础上完成角色库、统一 Agent Runtime、ComfyUI 工作流配置和自动/手动生图链路的整体升级。

发布日期：2026-09-09。V1.4.3 将已确认可用的内部版 V1.4.239 提升为正式版，业务功能保持一致。版本号遵循本项目“正式版 + 内部迭代序号”的规则，数字位数变化不表示降级。

## 主要更新

- 新增 34,122 个本地角色资料，包含角色名称、作品出处、身份 Tag 和可选外貌 Tag。角色专用词保持独立，不污染普通 Tag 搜索；歧义角色会在对话中显示选择卡片。
- 重构主 AI 调度：主 AI 只负责理解、检索和调度，Tag 编译、识图、候选评价、ComfyUI 渲染和迭代由固定工具与状态机执行。
- 新增自动/手动双模式生图。每轮支持 1–10 张，最大 ComfyUI 调用 1–10 次；候选按轮次横向排列，支持评价、反馈、提示词修订和最终选择。
- 新增候选图评估与横向比较。用户选择最终图片后立即终止后续工作；无变化修订和重复提示词会在提交前拦截。
- ComfyUI 独立配置页支持 API 工作流导入、节点分析、配置档、提示词/尺寸/批量覆盖和显式参考图绑定。未绑定参考图时明确标记为文本近似复刻。
- 新增调用监视器，记录主 AI、工具、Vision、Tag 和评价子代理的脱敏输入输出、事件和 Token；长记录独立滚动，隐藏密钥、图片像素和已识别的本地路径。
- 翻译页新增稳定 ID 对照协议和双向高亮。支持原文/译文选区、悬停、点击固定、重复词、语序变化、一对多/多对一及标点缺口恢复。
- AI 翻译关闭思维链和流式输出。翻译期间按钮显示旋转加载图标，完成、失败、超时或取消后恢复可用状态。
- 图片管理区分对话引用和独立图库，新增清空当前对话图片、删除对话时默认清理图片等操作。
- 角色库轻量计数、按需加载、普通 Tag 搜索减负和页面视图拆分，改善启动和搜索响应；非最大化窗口的导航居中，标题保留在左上角。
- 提示词组扩展为主 AI、文生图 Tag、画师与质量参考、识图、候选评价、翻译 6 项，支持编辑和导入导出。

## 从 V1.4.191 迁移

V1.4.3 使用新的统一 Runtime、角色资料和 ComfyUI 配置档。当前数据目录为 `%APPDATA%\ai-tag-toolbox-rewrite`。V1.4.191 使用的旧设置字段与新版不同，不保证自动迁移全部配置、对话和图片引用。升级前请保留旧版及其用户数据备份，导出需要保留的提示词与工作流；启动新版后检查 API、模型和工作流节点绑定，必要时重新配置。V1.4.239 升级到本正式版无需数据格式转换。

旧版外部 Agent Server/Runtime/Admin 写入接口已移除。主 AI 通过新的内部工具注册表调度，调用监视器用于本地调试和日志导出。

## 验证与限制

- `npm run check`：172 项通过，0 失败。
- 便携包校验 `app/`、`resources/app/`、`resources/app.asar` 和 13 个原生依赖。
- 本次仅提升版本号、整理文档及发布打包，未重新调用外部付费 AI 或运行 ComfyUI 出图；此前内部迭代的接口模拟、浏览器验证和用户试用结果保留。实际生图效果和翻译质量取决于配置模型。
- 角色资料是公开数据快照，不包含缩略图、原图或 LoRA；复杂 ComfyUI 工作流必须明确绑定参考图和可写节点。

下载：[AI.Tag.V1.4.3.7z](https://github.com/star-abyss/ai-tag-toolbox/releases/download/v1.4.3/AI.Tag.V1.4.3.7z)

整包包含 Windows x64 程序、Electron 与原生依赖、本地识图/翻译模型、Tag/角色素材及源码。解压后双击 `AI绘画Tag工具箱V1.4.3.exe`。同一 Release 提供 SHA-256 校验文件和构建记录；GitHub 自动生成的 Source code 压缩包仅含源码，不能直接运行。
